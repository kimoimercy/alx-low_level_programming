#include "main.h"
/**
 * print_last_digit - prints last digit of a number
 *
 * Return:the last digit/result output
 */
int print_last_digit(int)
{
	int x;

	if (n < 0)
		n = -n;
	x = n % 10
		_putchar (x + '0');
	return (x);
}
